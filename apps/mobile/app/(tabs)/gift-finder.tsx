import { Text, View } from "react-native";

export default function GiftFinder() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-2xl font-semibold">Gift Finder</Text>
      <Text className="mt-2 text-gray-500">The recipient / occasion / budget wizard lands in Stage 5.</Text>
    </View>
  );
}
