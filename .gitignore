node_modules/
.expo/
dist/
build/
.env
.env.local
*.log
.DS_Store
.supabase/

.vercel
.env*
supabase/.temp/

# Next.js / TypeScript build output
.next/
*.tsbuildinfo

# Database backups contain real customer names, phones and addresses.
cado-backups/

# Backup workflow scratch dirs — dumps must never land in the app repo
work/
backups/

# Credentials and working files — never committed
scratchpad/
